# IPL Tournament Scheduler (GTK + C) 🏏

A C-based IPL-style tournament scheduling system with a GTK graphical interface.
The system generates match schedules, manages points tables, and stores data using CSV files.

## Features
- IPL-style match scheduling
- GTK-based GUI
- CSV-based data storage
- Points table generation

## Technologies Used
- C
- GTK
- CSV files (Excel-compatible)

## Files Overview
- `src/` – C source files (scheduler + GTK GUI)
- `data/` – Match schedules and points tables
- `assets/` – Images used in the GUI
- 
## Author
Created by **Shree Vishali K**
